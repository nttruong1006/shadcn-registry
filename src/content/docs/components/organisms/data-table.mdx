---
title: Data Table
description: Powerful table and data grids built using TanStack Table.
---

import CodePreview from '@/components/app/code-preview/index.astro'
import InstallationCard from '@/components/app/installation-card/index.astro'
import ShadcnLinkButton from '@/components/app/shadcn-link-button/index.astro'

<CodePreview path='organisms/data-table/demo' />

## Installation
<InstallationCard registryName='data-table' />

## Usage
### Client Side Pagination
```tsx
import { type ColumnDef, getPaginationRowModel } from '@tanstack/react-table'
import { DataTable } from '@/components/organisms/data-table/data-table'
import { useDataTable } from '@/components/organisms/data-table/lib'
```

```tsx
const table = useDataTable({
  columns: COLUMNS,
  data: DATA,
  getPaginationRowModel: getPaginationRowModel()
})

<DataTable table={table} />
```
### Server Side Pagination
```tsx
import { type ColumnDef, type PaginationState} from '@tanstack/react-table'
import { DataTable } from '@/components/organisms/data-table/data-table'
import { useDataTable } from '@/components/organisms/data-table/lib'
```

```tsx
const rowCount = useState(0)
const [pagination, setPagination] = useState<PaginationState>({
  pageIndex: 0,
  pageSize: 10
})

const table = useDataTable({
  columns: COLUMNS,
  data: DATA,
  manualPagination: true,
  rowCount,
  state: { pagination },
  onPaginationChange: setPagination
})

<DataTable table={table} />
```

## Examples
### Column spanning
<CodePreview path='organisms/data-table/column-spanning' />

### Expanding
<CodePreview path='organisms/data-table/expanding' />

### Action column
<CodePreview path='organisms/data-table/action-column' />

### Selection
<CodePreview path='organisms/data-table/selection' />

### Column Visibility
<CodePreview path='organisms/data-table/column-visibility' />

### Column Pinning
<CodePreview path='organisms/data-table/column-pinning' />