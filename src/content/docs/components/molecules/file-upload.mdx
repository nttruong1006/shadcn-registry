---
title: File Upload
description: A file upload component with drag and drop or upload files.
---


import CodePreview from '@/components/app/code-preview/index.astro'
import InstallationCard from '@/components/app/installation-card/index.astro'
import ShadcnLinkButton from '@/components/app/shadcn-link-button/index.astro'
import ServerToaster from '@/components/app/server-toaster/index.astro'

<ServerToaster />

<CodePreview path='molecules/file-upload/demo' />

## Installation
<InstallationCard registryName='file-upload' />

## Usage
```tsx
import {
  FileUpload,
  FileUploadContent,
  FileUploadInput,
  FileUploadItem,
  type FileUploadProps
} from '@/components/molecules/file-upload'
```

```tsx
const [value, setValue] = useState<FileUploadProps['value']>([])

<FileUpload
  value={value}
  dropzoneOptions={{
    maxFiles: 5
  }}
  onValueChange={setValue}
>
  <FileUploadInput />
  <FileUploadContent>
    {value.map((item, index) => (
      <FileUploadItem key={`${index}-${item instanceof File ? item.name : item.id}`} value={item} index={index} />
    ))}
  </FileUploadContent>
</FileUpload>
```