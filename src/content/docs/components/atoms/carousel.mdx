---
title: Carousel
description: A carousel with motion and swipe built using Embla.
---

import CodePreview from '@/components/app/code-preview/index.astro'
import InstallationCard from '@/components/app/installation-card/index.astro'
import ShadcnLinkButton from '@/components/app/shadcn-link-button/index.astro'

<ShadcnLinkButton registryName='carousel' />

<CodePreview path='atoms/carousel/demo' />

## Installation

<InstallationCard registryName='carousel' />

## Usage

```tsx
import {
  Carousel,
  CarouselContent,
  CarouselItem,
  CarouselNext,
  CarouselPrevious,
} from "@/components/atoms/carousel"
```

```tsx
<Carousel>
  <CarouselContent>
    <CarouselItem>...</CarouselItem>
    <CarouselItem>...</CarouselItem>
    <CarouselItem>...</CarouselItem>
  </CarouselContent>
  <CarouselPrevious />
  <CarouselNext />
</Carousel>
```

## Examples
### Sizes
To set the size of the items, you can use the `basis` utility class on the `CarouselItem`.
<CodePreview path='atoms/carousel/sizes' />

### Spacing
To set the spacing between the items, we use a `pl-[VALUE]` utility on the `CarouselItem` and a negative `-ml-[VALUE]` on the `CarouselContent`.
<CodePreview path='atoms/carousel/spacing' />

### Orientation
Use the `orientation` prop to set the orientation of the carousel.
<CodePreview path='atoms/carousel/orientation' />

### API
Use a state and the `setApi` props to get an instance of the carousel API.
<CodePreview path='atoms/carousel/api' />

### Plugins
You can use the `plugins` prop to add plugins to the carousel.
<CodePreview path='atoms/carousel/plugins' />