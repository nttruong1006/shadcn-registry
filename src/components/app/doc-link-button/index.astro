---
import DocLinkButton from './_module'

// Props
interface Props {
  className?: string
  link: string
}

const { link, className } = Astro.props
---

<!-- Template -->
<DocLinkButton className={className} link={link} />
